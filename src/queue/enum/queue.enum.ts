export enum QueueProcessorType {
  CHAT_SIMULATION = 'chat-simulation',
}

export enum QueueProcessType {
  EPISODE_SCHEDULING = 'episode-scheduling',
  GENERATE_SIMULATION = 'generate-simulation',
  GENERATE_PODCAST = 'generate-podcast',
}
